# LXLang
LX Language<br/>
<img src="LXL.png" alt="LXL Logo" width="64"/>

## Compiling
You need:
* gcc
* make

Packages.<br/>
Use `cd src && make build` to build LXL.

## Running.
Run `./lxl`.

## Examples
Check `examples/`.
